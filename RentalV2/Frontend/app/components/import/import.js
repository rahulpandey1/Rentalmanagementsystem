app.controller('ImportController', function ($scope) {
    $scope.message = 'Data import is managed by the Rental Importer tool. ' +
        'Please run the importer separately to import data from Excel files.';
});
